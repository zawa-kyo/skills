# エージェントガイド

## リポジトリの構成

このリポジトリは、再利用可能なエージェントスキルを `apm` で公開する。`dev/` または `writing/` 配下の各ディレクトリは、それぞれ独立したスキルパッケージとして扱う。

各スキルには次のファイルを置く。

- `SKILL.md` は英語版のスキル定義。
- `SKILL-ja.md` は同じ挙動を説明する日本語版のスキル定義。
- `agents/openai.yaml` は OpenAI 向けの表示メタデータと default prompt を定義する。

## ドキュメントの同期

このリポジトリでテキストを生成、編集する場合は、同じ内容を扱う英語の Markdown ファイルと、それに対応する日本語の `*-ja.md` ファイルが存在するかを確認する。どちらか一方が存在する場合は、同じ変更の中で両方を更新し、内容の対応を保つ。

あわせて、ディレクトリ名に `-ja` を付けて日本語側を表している組み合わせも確認すること。たとえば `references/`/`references-ja/` や `maintenance/`/`maintenance-ja/` などが該当する。こうした組み合わせは、ディレクトリ単位でも英語版と日本語版の対応関係として扱う。片側のファイルを編集したときは、もう片側にも同じ内容を表す対応ファイルがあるか確認する。対応ファイルがあるなら、それも同じ変更の中で更新する。

英語版と日本語版は意味を揃えた上で、それぞれの言語として自然に書く。日本語として自然な表現で同じ意図を保てる場合は、英語版を機械的に一行ずつ翻訳した文章にしない。

このルールは、特に次のファイルに適用する。

- `README.md` と `README-ja.md`
- `AGENTS.md` と `AGENTS-ja.md`
- 各 `dev/<skill>/SKILL.md`、`dev/<skill>/SKILL-ja.md`、`writing/<skill>/SKILL.md`、`writing/<skill>/SKILL-ja.md`
- `<dir>/...` と `<dir>-ja/...` のような対応ディレクトリ配下の対応ファイル

## メンテナンス

スキルを追加、改名、削除する場合は、そのスキルに触れているリポジトリ内のファイルをすべて更新する。

- 対応する `dev/<skill>/` や `writing/<skill>/` ディレクトリの内容
- `README.md` と `README-ja.md` のインストール例とスキル一覧
- パッケージ単位の情報が変わる場合は `apm.yml`
- 表示名、short description、default prompt を変える必要がある場合は `agents/openai.yaml`

スキルの指示は、実際の作業で使える具体的な内容を書く。広い助言よりも、具体的なワークフロー、起動条件、出力への期待、検証手順を優先する。

対応する `SKILL.md` と `SKILL-ja.md` では、frontmatter の `name` を同一に保つ。`description` は自然にローカライズするが、起動意図と対象範囲は同じにする。

## `SKILL.md` の指針

- 記載は可能な限り 300 行以下に抑える。これを超えそうな場合は、本体を広げる前に、補助的な内容を別ファイルへ分割する構成を提案する。
- `SKILL.md` 本体は、処理全体のオーケストレーションを担う。詳細な手順やテンプレート、参考情報などは `references/` へ分割する。ただし、`SKILL.md` が肥大化していない限りは 1 ファイルのままで構わない。
- エージェントだけでなく人間にも読みやすいように構造化する。抽象的な指針から具体的な指示へ流れる構成を優先する。
- 日本語版のスキル定義は、原則として `である` / `する` の常体で書く。ただし、硬すぎる文章にならないよう読みやすさにも配慮する。`です` / `ます` の敬体は、ユーザーに返す出力例など、敬体であること自体が自然な箇所に限って残す。
