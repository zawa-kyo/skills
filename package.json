{
  "name": "agent-skills",
  "private": true,
  "scripts": {
    "format": "prettier \"**/*.md\" --write",
    "format:check": "prettier \"**/*.md\" --check"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
