interface:
  display_name: "Guide Integration Tests"
  short_description: "Handle assigned integration test responsibilities"
  default_prompt: "Use $guide-integration-tests to turn an assigned integration risk into tests with explicit boundaries, dependency fidelity, test data, and observable outcomes."
