# 参照元

このファイルは、外部資料やローカルな知見をどう使ったかを記録する。
スキルの実行時に読むガイドではなく、メンテナ向けの記録である。

## 参照元グループ

`references/` に出典ブロックを置かずに済むよう、実行時ルールは参照元グループで対応付ける。

### `khorikov-core`

著者: Vladimir Khorikov。

対応ルールは次のとおり。

- `UT-PUR-001` から `UT-PUR-004`
- `UT-CTX-002` から `UT-CTX-004`
- `UT-STR-001` から `UT-STR-005`
- `UT-DES-002` から `UT-DES-005`
- `UT-IMP-002` から `UT-IMP-006`

役割: このスキルの基本用語と標準的なルール群の参照元。

## 採用した考え方

- 単体テストは、持続可能なプロジェクト成長を支える。
- 良いテストは、リグレッションに対する保護、リファクタリング耐性、迅速なフィードバック、保守性のバランスで評価する。
- カバレッジは強い品質保証ではない。低すぎる場合に見直しの手がかりとして使える。
- テストは実装詳細より、外から確認できる振る舞いを重視する。
- スタブは検証しない。
- モックは重要なシステム境界で特に価値を持つ。
- テストしにくいコードは設計問題を示すことがある。

### `workflow-adaptations`

参照元: 上記の基本方針を、ローカルなスキル設計としてワークフロー化したもの。

対応ルールは次のとおり。

- `UT-CTX-001`
- `UT-DES-001`
- `UT-IMP-001`
- `UT-REV-001` から `UT-REV-003`

役割: トピック集ではなく、目的からレビューまでの思考順序としてスキルを組み立てるためのローカル調整。

## 調整して採用した考え方

- 古典学派は、多くのドメイン中心のシステムで有用なデフォルトとして扱う。唯一の正解とはしない。
- ドメインロジックは価値の高い対象として扱う。単体テスト可能な対象をそこだけに限定しない。
- 結合テストとデータベーステストの内容は、単体テストの境界を明確にする範囲でだけ使う。
- エージェントが局所的には正しいが、プロジェクトに合わないテストを作らないよう、戦略の前にコンテキスト確認を置く。
- レビューは別の論点集にせず、同じワークフローを圧縮して使う。

## 採用しない、または保留した内容

- フレームワーク固有の例は中心的な参照資料へ持ち込まない。
- 冗長な例示や背景的な説明は、ドキュメントに反映しない。
- ただし、サンプルコード等による端的な例示は、ルールの理解を助け、スキルの動作を特定のフレームワーク向けに限定しない範囲で許容する。

## 未解決の論点

- フィクスチャやテストデータパターンのために _xUnit Test Patterns_ を追加するか。
- フレームワークに依存しない基本方針を弱めずに、言語固有の注記を後から追加するか。
