# ルール整理メモ

このファイルは、ルールを追加、修正、削除するときの保守用メモだ。
スキルの実行時に読む資料ではなく、どの工程にどの判断を置くかを確認するために使う。

参照元や採用判断の理由は `sources.md` にまとめる。
このファイルには、細かな資料対応を入れない。

## 書き方

ルールを追加するときは、次の観点で書く。

- Rule ID: 保守メモと参照元対応で使う安定した識別子。
- 置き場所: そのルールを記載する実行時資料。
- 優先度: Essential / Recommended / Suggested。
- 参照元グループ: そのルールの元になった参照元や調整方針。
- 判断内容: 実行時資料で利用者に見せるルールや確認項目。
- 補足: 保守者だけが必要とする採用理由、例外、移行メモ。

適用場面は、原則としてワークフローのファイル名と見出しから読めるようにする。別途説明しないと適用場面が分からないなら、ファイル構造が役割を十分に表せていない可能性が高い。

## 目的 (`references/01-purpose.md`)

### UT-PUR-001

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 単体テストは、テスト数やカバレッジの最大化ではなく、持続可能な変更を支えるために書く。
補足: カバレッジ起点の依頼を言い換えるときの基準。

### UT-PUR-002

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: テストは、意味のある振る舞いを守り、保守コストに見合うときだけ残す。
補足: テストは無料の資産ではない、という論点を含む。

### UT-PUR-003

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 単体テストは、リグレッションに対する保護、リファクタリング耐性、迅速なフィードバック、保守性で評価する。
補足: 実行時資料ではメタ情報としてではなく評価軸として見せる。

### UT-PUR-004

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: カバレッジは、警告信号として使い、テストを書く理由そのものにはしない。
補足: カバレッジだけを理由にした依頼へ対応するときに使う。

## 文脈 (`references/02-context.md`)

### UT-CTX-001

優先度: Essential。
参照元グループ: `workflow-adaptations`。
判断内容: 既存プロジェクトのコードに対しては、ドメイン、アーキテクチャ、依存関係、既存テストの文脈を読んでからテストを設計する。
補足: プロジェクト文脈を前提にするワークフロー上の追加ルール。

### UT-CTX-002

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: 価値のある振る舞いがテストしにくい場合は、モックやテスト専用の逃げ道を増やす前に設計上の問題を疑う。
補足: ただし、インフラや並行処理には本質的な難しさもある。

### UT-CTX-003

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: 重要な判断と副作用は、可能な範囲で分ける。
補足: 実行時資料では抽象論より具体的な設計判断として書く。

### UT-CTX-004

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: 非公開メソッドを直接テストするのではなく、必要なら意味のある概念として抽出する。
補足: 文脈を読む段階で設計上の問題として扱う。

## テスト戦略 (`references/03-test-strategy.md`)

### UT-STR-001

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: コードを書く前に、テストレベルと単体テストの境界を決める。
補足: 適用場面はファイルの責務で示す。

### UT-STR-002

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: テスト対象の単位は、クラス構造ではなく振る舞いで決める。
補足: 高速で独立しているなら、1つの振る舞いが複数クラスにまたがってもよい。

### UT-STR-003

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 壊れたときの影響が大きい振る舞いを優先して単体テストする。
補足: 単純なコードは、より価値の高いテストから間接的に通れば足りることがある。

### UT-STR-004

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 共有依存と不安定な依存は、単体テストでは置き換えるか制御する。
補足: 安定したプロセス内の協力オブジェクトは、必要以上に置き換えない。

### UT-STR-005

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: 検証方法は、出力、状態、相互作用の順をデフォルトにして選ぶ。
補足: 実行時資料では、絶対ルールではなくデフォルト順として示す。

## テスト設計 (`references/04-test-design.md`)

### UT-DES-001

優先度: Essential。
参照元グループ: `workflow-adaptations`。
判断内容: 実装前に、戦略を具体的なテストケースへ落とす。
補足: 戦略と実装の間に明示的な設計工程を置くための追加ルール。

### UT-DES-002

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 実装詳細ではなく、外から確認できる結果を優先して検証する。
補足: 公開契約そのものなら例外を認める。

### UT-DES-003

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: スタブを検証しない。
補足: モックの扱いとは別ルールとして分けておく。

### UT-DES-004

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: 重要な境界通信だけをモックする。
補足: アダプタ中心の処理は、結合テストで扱うほうがよいこともある。

### UT-DES-005

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: 非公開の振る舞いは、公開された振る舞いを通じてテストする。
補足: `UT-CTX-004` と関連するが、こちらはケース設計時の確認事項。

## 実装 (`references/05-implementation.md`)

### UT-IMP-001

優先度: Essential。
参照元グループ: `workflow-adaptations`。
判断内容: 設計したケースを、その目的を崩さずに実装する。
補足: フレームワーク都合でテストの意味を変えないための受け渡しルール。

### UT-IMP-002

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: テスト名は、振る舞いと期待結果が分かるように付ける。
補足: メソッド名をなぞるだけの命名を避ける。

### UT-IMP-003

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: テストは Arrange、Act、Assert で構成する。
補足: ごく小さなテストでは、ラベルを省いてもよい。

### UT-IMP-004

優先度: Recommended。
参照元グループ: `khorikov-core`。
判断内容: テストは直線的に保ち、アサーションに本番コードのようなロジックを入れない。
補足: 各ケースが明確なら、パラメータ化テストは認める。

### UT-IMP-005

優先度: Suggested。
参照元グループ: `khorikov-core`。
判断内容: セットアップは、共有フィクスチャより明示的なビルダーやファクトリで再利用する。
補足: 一貫性のためのデフォルトであり、絶対ルールではない。

### UT-IMP-006

優先度: Essential。
参照元グループ: `khorikov-core`。
判断内容: テストのためだけに本番コードを汚染せず、本番モデルも改善する設計変更を優先する。
補足: テスト専用の分岐、公開状態、緩い契約を含む。

## セルフレビュー (`references/06-self-review.md`)

### UT-REV-001

優先度: Essential。
参照元グループ: `workflow-adaptations`。
判断内容: 目的、文脈、戦略、設計、実装の同じワークフローでテストを見直す。
補足: レビューは別の作法ではなく、同じ流れを圧縮したものだ。

### UT-REV-002

優先度: Essential。
参照元グループ: `workflow-adaptations`。
判断内容: レビューでは、行単位のスタイルより先に、振る舞いとリスクを見る。
補足: 指摘をテスト価値に結び付けるためのルール。

### UT-REV-003

優先度: Recommended。
参照元グループ: `workflow-adaptations`。
判断内容: 指摘は、違反している原則、具体的なリスク、最小限の有効な修正方針と一緒に報告する。
補足: 実行時資料には、アンチパターンや問題なしの場合の扱いも含める。
