---
name: review-essential-code
description: コード変更が根本原因を解決しているか、AI らしい場当たり的な回避策になっていないかをレビューする。バグ、リグレッション、テスト不足、過剰な分岐や例外処理、重複ロジックなどを確認し、具体的なリスクと最小の修正方針を報告する。コードレビューを求められたときに使う。
---

# 本質的なコードレビュー

## 目的

コード変更が、既存の設計に沿って根本原因を解決しているかを確認する。
観測された失敗を隠すだけの特殊ケース、重複ロジック、過剰なガード、不要な抽象化など、AI にありがちな場当たり的な修正を見抜く。

## ワークフロー

1. レビュー対象と範囲を特定する。指定がなければ、現在の Git 差分をステージング済みと未ステージングに分けて確認する。レビュー中はワークツリーと index を変更しない。
2. 周辺コード、テスト、ドキュメントを読み、対象の振る舞い、既存の不変条件、公開契約、責務の境界、ローカルな設計パターンを理解する。
3. 変更が解決しようとしている問題と、実際に変更された経路を確認する。変更の責務を持つ境界がどこかを特定する。
4. 通常のリスクを確認する。バグ、リグレッション、データ損失、セキュリティ、性能、API 契約、テスト不足を対象にする。
5. 本質性を確認する。
   - 根本原因を解決しているか。失敗を抑え込んでいるだけではないか。
   - 既存のルールや責務の境界を使っているか。特定の入力だけを特別扱いしていないか。
   - 複雑さや重複を減らしているか。分岐、フラグ、リトライ、待機、広い例外処理、型のごまかしなどを足していないか。
   - 呼び出し側の振る舞いと、実装を保守する理由が明確か。
6. 具体的な箇所と根拠のある問題だけを指摘する。仮説にとどまるものは未解決の問いとして分け、問題がなければそう明示する。

## テスト

テストの境界やテストレベルの選択自体が問題になる場合は、`guide-automated-tests` または対応する専門スキルを使う。テスト形式の好みだけは指摘しない。

## 出力

重大度順に、ファイルと行、具体的なリスク、根拠、最小の修正方針を示す。
finding として断定できない前提、未解決の問い、実行しなかった確認は、必要な場合だけ後に分けて書く。
ユーザーが明示的に求めない限り、コードは変更しない。ユーザーの言語で報告する。
