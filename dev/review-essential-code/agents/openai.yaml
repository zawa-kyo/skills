interface:
  display_name: "Review Essential Code"
  short_description: "Review code for root causes and ad hoc fixes"
  default_prompt: "Use $review-essential-code to check whether this change fixes the root cause or adds an ad hoc workaround."
